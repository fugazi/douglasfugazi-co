[build]
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.33.0"

[context.deploy-preview]
  command = "pnpm run build"
  publish = "dist"
